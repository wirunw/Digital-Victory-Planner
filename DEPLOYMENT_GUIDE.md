# 🚀 คู่มือการ Deploy บน Netlify

## ขั้นตอนที่ 1: เตรียม Repository

1. **Push code ไปที่ GitHub**
```bash
git init
git add .
git commit -m "Initial commit: Digital Victory Planner with AI Assistant"
git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main
```

## ขั้นตอนที่ 2: ตั้งค่า Netlify

### 2.1 เชื่อมต่อกับ Netlify

1. ไปที่ [Netlify Dashboard](https://app.netlify.com/)
2. คลิก "Add new site" → "Import an existing project"
3. เลือก GitHub provider
4. เลือก repository ของคุณ

### 2.2 ตั้งค่า Build Settings

ในหน้า Site settings → Build & deploy:

```
Build command: npm run build
Publish directory: . (root directory)
```

### 2.3 ตั้งค่า Environment Variables (สำคัญมาก!)

ไปที่ Site settings → Environment variables → Add variable:

```
Variable name: TYPHOON_API_KEY
Value: [ใส่ Typhoon API Key ของคุณที่นี่]
```

```
Variable name: TYPHOON_BASE_URL
Value: https://api.opentyphoon.ai/v1
```

**⚠️ คำเตือน**: อย่าเคยเก็บ API Key ใน code หรือ commit ขึ้น GitHub!

## ขั้นตอนที่ 3: Deploy

1. คลิก "Deploy site" บน Netlify
2. รอจนกว่า deployment เสร็จ (ประมาณ 2-3 นาที)
3. ทดสอบการทำงานของเว็บไซต์

## ขั้นตอนที่ 4: ทดสอบ AI Assistant

หลัง deploy สำเร็จ:

1. เปิดเว็บไซต์ที่ Netlify ให้
2. คลิกปุ่ม AI Assistant (มุมขวาล่าง)
3. ลองพิมพ์ข้อความทดสอบ เช่น "สวัสดี ช่วยแนะนำชื่อสินค้าหน่อย"
4. ตรวจสอบว่า AI ตอบกลับมาหรือไม่

## การแก้ไขปัญหาที่พบบ่อย

### ❌ AI Assistant ไม่ทำงาน

**สาเหตุ**: น่าจะลืมตั้งค่า Environment Variables

**วิธีแก้**:
1. ตรวจสอบว่าตั้งค่า `TYPHOON_API_KEY` แล้วหรือไม่
2. ตรวจสอบว่า API Key ถูกต้องหรือไม่
3. ดูที่ Netlify Functions logs ใน Dashboard

### ❌ Function Error

**สาเหตุ**: Netlify Function ไม่สามารถเรียก Typhoon API

**วิธีแก้**:
1. ตรวจสอบ `netlify/functions/ai-chat.js` ว่าอยู่ถูกที่
2. ตรวจสอบ `netlify.toml` configuration
3. ลอง redeploy ใหม่

### ❌ CORS Error

**สาเหตุ**: Browser ไม่อนุญาตให้เรียก API ข้าม domain

**วิธีแก้**: ตรวจสอบว่ามี CORS headers ใน `ai-chat.js` หรือไม่

## การอัพเดต Code

หลังจากแก้ไข code:

```bash
git add .
git commit -m "Update: [รายละเอียดการแก้ไข]"
git push
```

Netlify จะทำการ auto-deploy อัตโนมัติ

## การตรวจสอบ Logs

1. ไปที่ Netlify Dashboard
2. เลือก site ของคุณ
3. ไปที่ Functions → View function logs
4. ดู error messages ถ้ามีปัญหา

## 🎯 การตรวจสอบว่าทำงานถูกต้อง

- [ ] เว็บโหลดได้ปกติ
- [ ] สามารถเปลี่ยน tab ระหว่าง Blueprint และ Workshop App
- [ ] กรอกข้อมูลในแบบฟอร์มได้
- [ ] AI Assistant ตอบกลับได้
- [ ] สามารถส่งออกเป็น PDF และ TXT ได้
- [ ] Responsive design ทำงานบนมือถือ

## 📞 ติดต่อสอบถาม

ถ้ามีปัญหาในการ deploy:
- ตรวจสอบ Netlify documentation: https://docs.netlify.com/
- ตรวจสอบ Typhoon API documentation: https://opentyphoon.ai/
- ติดต่อผู้พัฒนา: Wirun Wetsiri
